// This scene is no longer used — GameLevel replaces it.
// Kept as an empty stub to avoid import errors.
import { Scene } from 'phaser';
export class Game extends Scene {
    constructor() { super('Game'); }
}
